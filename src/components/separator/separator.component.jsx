import react from "react";
import { SeparatorContainer } from "./separator.styles";

const Separator = ({ width, margin }) => (
  <SeparatorContainer width={width} margin={margin} />
);

export default Separator;
